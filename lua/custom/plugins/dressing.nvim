return {
  'stevearc/dressing.nvim',
  opts = {
    select = {
      backend = { 'telescope' },  -- Wymaga telescope.nvim
      telescope = {
        layout_strategy = 'vertical',  -- Kolumny!
        layout_config = {
          height = 0.8,               -- Wysokość menu
          width = 0.8,                -- Szerokość menu
        },
      },
    },
  },
}
